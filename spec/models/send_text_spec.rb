# NOTE - test switched off for now as it only works if the promises database has a promise with an end date that matches today's date
# TO DO - set up this test to use mocking instead

# require_relative '../../app/models/send_text'
#
# describe 'sending a text' do
#   it 'gets info from database to insert into text message' do
#     expect(get_info_and_send_text).to eq 'text sent'
#   end
# end
